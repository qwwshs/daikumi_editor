# Daikumi Editor 用户指南

## 目录

1. [简介](#简介)
2. [安装与启动](#安装与启动)
3. [界面概览](#界面概览)
4. [基本操作](#基本操作)
5. [谱面编辑](#谱面编辑)
6. [快捷键](#快捷键)
7. [插件系统](#插件系统)
8. [导入导出](#导入导出)
9. [常见问题](#常见问题)

---

## 简介

Daikumi Editor 是一款基于 LOVE2D 框架开发的音乐游戏谱面编辑器，主要用于创建和编辑音游谱面。编辑器支持多种音符类型、事件编辑、BPM 变化等功能，并提供了插件系统以扩展功能。

### 主要功能

- 多轨道音符编辑（note、hold、wipe）
- 事件系统（x、w、lpos、rpos）
- BPM 变化和线性渐变
- 贝塞尔曲线和缓动函数
- 实时预览和打击效果
- FFT 频谱分析
- 均衡器控制
- 导出为 Takana 格式

---

## 安装与启动

### 系统要求

- Windows 7 及以上
- LOVE2D 11.4

### 安装步骤

1. 下载并安装 [LOVE2D 11.4](https://love2d.org/)
2. 下载 Daikumi Editor 发行包
3. 解压到任意目录

### 启动方式

**方式一：双击运行**
- 直接双击 `daikumi-editor.exe`

**方式二：命令行运行**
```bash
love "路径/到/daikumi editor"
```

### 首次启动

首次启动时，编辑器会在程序目录下创建 `users/` 文件夹，用于存储：
- 用户设置
- 快捷键配置
- 日志文件
- 导出文件

---

## 界面概览

### 主界面布局

```
+--------------------------------------------------+
|  菜单栏 (Menu Bar)                                |
+--------------------------------------------------+
|        |                                    |     |
|        |         编辑区域                    |     |
|  轨道   |        (Play Area)                | 侧  |
| 列表   |                                    | 边  |
|        |                                    | 栏  |
|        |                                    |     |
+--------------------------------------------------+
|  状态栏 / BPM / 时间信息                         |
+--------------------------------------------------+
```

### 界面区域说明

1. **菜单栏**：包含文件操作、编辑工具、设置等
2. **编辑区域**：主工作区，显示音符和事件
3. **轨道列表**：显示所有轨道，可切换当前轨道
4. **侧边栏**：显示选中对象的详细属性
5. **状态栏**：显示当前 BPM、时间、beat 等信息

---

## 基本操作

### 文件操作

| 操作 | 说明 |
|------|------|
| 新建谱面 | 创建空白谱面 |
| 打开谱面 | 打开已有的谱面文件 |
| 保存谱面 | 保存当前编辑的谱面 |
| 另存为 | 将谱面保存到新位置 |

### 编辑操作

| 操作 | 快捷键 | 说明 |
|------|--------|------|
| 撤销 | Ctrl+Z | 撤销上一步操作 |
| 重做 | Ctrl+Y | 重做上一步操作 |
| 复制 | Ctrl+C | 复制选中的音符 |
| 粘贴 | Ctrl+V | 粘贴音符到当前位置 |
| 删除 | Delete | 删除选中的音符 |

### 视图控制

| 操作 | 说明 |
|------|------|
| 鼠标滚轮 | 上下滚动编辑区域 |
| Ctrl+滚轮 | 缩放编辑区域 |
| 空格键 | 播放/暂停音乐 |

---

## 谱面编辑

### 音符类型

#### Note（普通音符）
- 最基础的音符类型
- 在指定 beat 位置点击即可放置

#### Hold（长按音符）
- 需要长按的音符
- 第一次点击放置头部，第二次点击放置尾部
- 头部可以是普通 note 或 wipe

#### Wipe（滑动音符）
- 需要滑动的音符
- 操作方式与 note 相同

### 放置音符

1. 选择音符类型（note/hold/wipe）
2. 在编辑区域点击鼠标左键放置
3. 对于 hold 类型：
   - 第一次点击放置头部
   - 第二次点击放置尾部
   - 尾部必须在头部之后

### 删除音符

1. 将鼠标移动到要删除的音符位置
2. 点击鼠标右键删除

### 编辑音符属性

1. 点击选中一个音符
2. 在侧边栏中修改属性：
   - **beat**：音符位置（格式：整数,分子,分母）
   - **track**：所在轨道
   - **fake**：是否为假音符（不计分）
   - **note_head**：hold 头部类型
   - **wipe_head**：是否为 wipe 头

---

### 事件系统

事件用于控制轨道的视觉效果，包括位置、宽度、左右边界等。

#### 事件类型

| 类型 | 说明 |
|------|------|
| x | 轨道中心位置 |
| w | 轨道宽度 |
| lpos | 轨道左边界 |
| rpos | 轨道右边界 |

#### 创建事件

1. 切换到事件编辑模式
2. 选择事件类型
3. 在编辑区域点击放置

#### 编辑事件属性

选中事件后，侧边栏显示：

- **beat**：事件起始位置
- **beat2**：事件结束位置
- **from/to**：事件值的变化范围
- **trans**：过渡类型（bezier/easings）
- **trans.trans**：贝塞尔控制点或缓动函数索引

---

### BPM 编辑

#### 添加 BPM 变化点

1. 在 BPM 列表中点击"添加"
2. 设置 beat 位置和 BPM 值
3. 选择是否启用线性渐变

#### BPM 属性

| 属性 | 说明 |
|------|------|
| beat | BPM 变化点位置 |
| bpm | BPM 值 |
| linear_ramp | 0=突变，1=线性渐变 |

---

## 快捷键

### 基础快捷键

| 快捷键 | 功能 |
|--------|------|
| Space | 播放/暂停 |
| Ctrl+Z | 撤销 |
| Ctrl+Y | 重做 |
| Ctrl+C | 复制 |
| Ctrl+V | 粘贴 |
| Ctrl+S | 保存 |
| Delete | 删除选中 |
| Escape | 取消选择 |

### 编辑快捷键

| 快捷键 | 功能 |
|--------|------|
| 1 | 选择 note 类型 |
| 2 | 选择 hold 类型 |
| 3 | 选择 wipe 类型 |
| Tab | 切换事件类型 |

### 自定义快捷键

快捷键配置文件位于 `users/key.json`，格式为 JSON：

```json
{
    "play_pause": ["space"],
    "undo": ["lctrl", "z"],
    "redo": ["lctrl", "y"],
    "copy": ["lctrl", "c"],
    "paste": ["lctrl", "v"],
    "save": ["lctrl", "s"],
    "delete": ["delete"]
}
```

修改后重启编辑器生效。

---

## 插件系统

Daikumi Editor 支持通过插件扩展功能。内置插件包括：

### 内置插件

#### 1. 均衡器 (Equalizer)
- **位置**：侧边栏
- **功能**：10 段参量均衡器，可调节音频频率响应
- **参数**：
  - 低频增益/频率/带宽
  - 低中频增益/频率/带宽
  - 高中频增益/频率/带宽
  - 高频增益/频率/带宽

#### 2. FFT 频谱分析器
- **位置**：菜单界面
- **功能**：实时显示音频频谱
- **使用**：选择音乐后自动加载

#### 3. Takana 转谱器
- **位置**：侧边栏
- **功能**：将谱面导出为 Takana 格式
- **参数**：
  - frame_rate：帧率（1-120）

#### 4. 打击效果 (Hit)
- **位置**：编辑区域
- **功能**：播放时显示打击视觉效果和音效
- **配置**：
  - settings.hit：是否显示打击效果
  - settings.hit_sound：是否播放打击音效
  - settings.hit_time：效果持续时间
  - settings.hit_volume：音效音量

#### 5. 直观事件编辑 (Direct Event Editing)
- **位置**：编辑区域
- **功能**：通过拖拽可视化编辑事件
- **使用**：
  1. 开启直观编辑模式
  2. 拖拽事件头尾圆圈调整位置
  3. 拖拽贝塞尔控制点调整曲线
  4. 右键菜单切换过渡类型

### 插件开发

如需开发自定义插件，请参考以下结构：

```lua
-- 插件基本结构
local myPlugin = {
    name = "my_plugin",
    version = "1.0.0",
    description = "我的自定义插件",
    
    -- 生命周期方法
    init = function(ctx)
        -- 初始化代码
    end,
    
    update = function(ctx, dt)
        -- 每帧更新
    end,
    
    draw = function(ctx)
        -- 绘制
    end,
    
    -- 事件钩子
    hooks = {
        onNoteAdd = function(ctx, note)
            -- 音符添加时触发
        end,
        
        onEventAdd = function(ctx, event)
            -- 事件添加时触发
        end,
    },
}

return myPlugin
```

---

## 导入导出

### 导出为 Takana 格式

1. 打开要导出的谱面
2. 在侧边栏找到"Takana 转谱器"
3. 设置帧率（默认 30）
4. 点击"导出"按钮
5. 导出文件保存在 `users/export/` 目录

#### 导出文件结构

```
export/
└── 歌曲名/
    ├── .t3proj          # 项目配置
    ├── songinfo.yaml    # 歌曲信息
    ├── preference.yaml  # 偏好设置
    ├── music.mp3        # 音频文件
    ├── cover.jpg        # 封面图片
    └── [难度].json      # 谱面数据
```

#### 难度自动识别

编辑器会根据谱面名称自动识别难度等级：
- normal（普通）
- hard（困难）
- master（大师）
- insanity（疯狂）
- ravage（毁灭）

---

## 常见问题

### Q: 编辑器无法启动

**A:** 请检查：
1. 是否安装了 LOVE2D 11.4
2. 是否以管理员权限运行
3. 查看 `users/log/` 目录下的日志文件

### Q: 如何修改界面语言？

**A:** 编辑器支持国际化，语言文件位于 `i18n/` 目录。修改 `users/settings.json` 中的语言设置。

### Q: 音频播放不同步

**A:** 尝试：
1. 调整音频偏移量（offset）
2. 检查音频文件格式是否支持
3. 重新加载音频文件

### Q: 如何备份谱面？

**A:** 谱面文件位于 `users/chart/` 目录，直接复制该文件夹即可备份。

### Q: 插件加载失败

**A:** 检查：
1. 查看日志中的错误信息
2. 确认插件文件格式正确
3. 检查插件依赖是否满足

### Q: 如何自定义打击音效？

**A:** 将自定义音效文件放入 `users/` 目录，文件名包含"hit"即可自动加载。

### Q: 如何自定义 UI 图片？

**A:** 将自定义图片放入 `users/ui/` 目录：
- 文件名包含"hit"：替换打击效果图片
- 文件名包含"hit_light"：替换打击光效图片

---

## 技术支持

- **GitHub**：https://github.com/qwwshs/daikumi/
- **官方网站**：https://dakumi.qwwshs.top

---

## 版本历史

### v2.0 (重构版)
- 插件系统架构
- 服务层解耦
- 代码注释完善
- 文档完善

### v1.x
- 初始版本

---

*本文档最后更新：2026-08-18*
