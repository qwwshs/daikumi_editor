# Dakumi 谱面格式 v1

Godot 游玩器与 `dakumi editor` 共用 JSON 格式。根对象的 `version` 当前为 `1`。

## 根字段

- `version`: 整数，当前为 `1`。
- `offset`: 音频偏移，单位毫秒。玩家侧另有两个同样以毫秒计的延迟（都不设上下限）：设置里的「音频延迟 / 时间偏移」对所有歌生效，选曲界面的「单曲延迟」只对那一首生效。三者之和就是最终的音频延迟：正值让音频推后、负值让音频提前，游玩时钟与开唱倒计时都只读这一个和（见 `ChartLoader.total_offset_seconds`）。
- `bpm_list`: BPM 段数组。
- `note`: 音符数组。
- `event`: 轨道事件数组。
- `effect`: 特效数组。
- `info`: `song_name`、`chart_name`、`chartor`、`artist`。
- `preference`: `x_offset` 与 `event_scale`，其中 `event_scale` 不可为 0。
- `track`: 以轨道 ID 字符串为键的轨道属性。

节拍使用 `[整数拍, 分子, 分母]`，例如 `[3, 1, 4]` 表示 `3.25` 拍。加载器也接受已经换算好的数字节拍。

## 音符

```json
{"type":"note","beat":[1,0,1],"track":1,"fake":0}
{"type":"wipe","beat":[2,0,1],"track":1,"fake":0}
{"type":"hold","beat":[3,0,1],"beat2":[4,0,1],"track":1,"fake":0,"note_head":0,"wipe_head":0}
```

编辑器类型为 `note`、`wipe`、`hold`。Godot 加载后会转成内部的 `Tap`、`Slide`、`Hold`，不会改写源文件。`fake`、`note_head`、`wipe_head` 缺失时按 `0` 处理。

## 轨道事件

```json
{
  "type":"x",
  "beat":[0,0,1],
  "beat2":[1,0,1],
  "track":1,
  "from":0,
  "to":50,
  "trans":{"type":"bezier","trans":[0,0,1,1],"easings":1}
}
```

事件类型支持 `x`、`w`、`lpos`、`rpos`。过渡的 `type` 支持 `bezier` 和 `easings`。

轨道在它的**首个事件开始之前不存在**：事件之前 `from` 只是过渡起点，游玩器不拿它当显示宽度。否则 `from` 写成 `0.1` 这类“隐形”占位值会在歌一开始凭空画出多余的轨道。希望轨道一开始就出现，就把首个事件的 `beat` 写成 0。

宽度只要不为 0 就照画：谱面常把宽度当成出场 / 消失的动画量，按阈值隐藏会让这个过程看起来是跳变的。宽度为 0 的轨道只剩一条细线（两侧描边重合），要当作可见的线状轨道就得配 `w0thenShow` 非 0；这种轨道给音符当立足点，但它同样要等首个事件开始。

游玩区域左右各画一条侧线（白色、透明度跟随 `lane_line_alpha`，笔画是轨道线的四倍）：贴在区域外侧，内边正好压在区域边界上，不遮住区域里的轨道；和轨道一样纵向铺满画布，一直画到屏幕顶端。判定线只横跨游玩区域：两端落在左右两条侧线上，不铺到画面边缘。

背景（`bg` 图片与底色）是普通 2D，铺满整个屏幕，不跟着轨道倾斜；倾斜后收敛的游玩区域两侧、画面角落露出的都是它。游玩区域一直铺到屏幕顶端（不会在中途截断或溶解）。

Note 的出现点固定是**屏幕顶端**：下落幅度取“判定线到屏幕顶端”的距离，与倾斜角度、接近曲线都无关，所以不管参数怎么调，Note 都不会在画面中途凭空冒出来。Note 从屏幕顶端落到判定线的时间就是 `10 / 流速` 秒（“流速”因此是屏幕上可核对的速度：数值越大越快、同屏出现的 Note 越少）。

## 兼容迁移

加载器会在内存中自动处理以下旧格式：

- 根对象缺少 `version`；
- 事件把 `from` 拼成 `form`；
- `trans` 直接写成贝塞尔数组；
- 音符缺少 `fake`，Hold 缺少头部字段；
- 缺少 `info`、`preference`、`track`、`effect` 或 BPM 列表。

每个 BPM、音符、事件、过渡和轨道对象也会逐项补齐缺失属性。谱面中出现但没有定义的轨道会自动生成完整的默认轨道配置。补全过程只修改内存中的运行时副本，不会覆盖原始 JSON；`ChartData.filled_properties` 会列出本次补充的属性路径。

不支持的事件类型会保留在谱面数据中，但不会进入轨道渲染索引。
